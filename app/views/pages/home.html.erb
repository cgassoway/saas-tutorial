<div class='text-center home-page container-fluid'>
  <h1>Welcome to DevMatch</h1>
  <h3>A membership site when entrepeneurs can meet developers</h3>
</div>

<div class="container home-signup-links">
  <div class="row">
    <% if user_signed_in? %>
      <div class="col-md-6">
        <div class="well">
          <h2 class="text-center">Your Profile </h2>
          <% if current_user.profile %>
            <%= link_to "Edit Your User Profile", edit_user_profile_path(user_id: current_user.id), class: "btn btn-default btn-lg btn-block" %>
            <%= link_to "View Your User Profile", user_path(id: current_user.id), class: "btn btn-default btn-lg btn-block" %>
          <% else %>
            <p class="text-center">Create your profile so you can share your information with the community</p>
            <%= link_to "Create profile", new_user_profile_path(user_id: current_user.id), class: "btn btn-default btn-lg btn-block" %>
          <% end %>
        </div>
      </div>
      <div class="col-md-6">
        <div class="well">
          <h2 class="text-center">Visit the Community</h2>
          <p class="text-center">Visit the community to meet other developers, enterpreneurs, and investors</p>
          <%= link_to "Visit the community", "#", class: "btn btn-default btn-lg btn-block" %>
        </div>
      </div>
    <% else %>
      <div class="col-md-6">
        <div class="well">
          <h2 class="text-center">Basic Membership Link</h2>
          <h4 class="bigbottom">Signup for free and get basic access to our community</h4>
          <br />
          <%= link_to "Signup with our Basic Plan", new_user_registration_path(plan: @basic_plan), class: 'btn btn-primary btn-lg btn-block' %>
        </div>
      </div>
      <div class="col-md-6">
        <div class="well">
          <h2 class="text-center">Pro Membership Link</h2>
          <h4>Signup for $10/month and get full access to our community and contact with other members</h4>
          <br />
          <%= link_to "Signup with our Pro Plan", new_user_registration_path(plan: @pro_plan), class: 'btn btn-success btn-lg btn-block' %>        
        </div>
      </div>
      <% end %>
  </div>

</div>